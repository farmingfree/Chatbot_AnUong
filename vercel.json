{
  "buildCommand": "cd apps/web && npm run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "cd apps/web && npm install",
  "framework": "nextjs",
  "rewrites": [
    {"source": "/api/internal/:path*", "destination": "https://your-api.railway.app/:path*"}
  ]
}
